//typescript requires data type automatically
var firstName: string = "Annie";
const increment = (counterVal: number) => {
    return counterVal + 1;
}

const counter: number = increment(77);

let programmingLanguages: string[] = ["Java", "Python", "JavaScript"];